#pragma once

/**
 * \brief Expand simple quotes as defined in the SCL
 * \param str The string to expand
 * \return The same string as the one passed in str
 */
char *expand_simple_quotes(char *str);